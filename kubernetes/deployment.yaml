apiVersion: apps/v1
kind: Deployment
metadata:
  name: air-temperature-regressor-deployment
  labels:
    app: temperature-air-regressor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: temperature-air-regressor
  template:
    metadata:
      labels:
        app: temperature-air-regressor
    spec:
      containers:
        - name: temperature-air-regressor
          image: europe-west6-docker.pkg.dev/mlops-miss-meteo/miss-meteo-registry/air-temperature-regressor:latest
